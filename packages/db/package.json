{
  "name": "@fft/db",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "dependencies": {
    "@prisma/client": "^5.16.1"
  },
  "devDependencies": {
    "prisma": "^5.16.1"
  },
  "prisma": {
    "schema": "prisma/schema.prisma"
  },
  "scripts": {
    "migrate": "prisma migrate dev --name init_users",
    "push": "prisma db push",
    "generate": "prisma generate",
    "studio": "prisma studio"
  }
}