{
  "name": "food-for-thought-2-0",
  "private": true,
  "workspaces": [
    "web",
    "mobile",
    "packages/*"
  ],
  "scripts": {
    "dev:web": "npm --workspace web run dev",
    "build:web": "npm --workspace web run build",
    "lint": "npm --workspace web run lint",
    "typecheck": "npm --workspace web run typecheck"
  },
  "devDependencies": {
    "prisma": "^6.14.0"
  },
  "dependencies": {
    "@prisma/client": "^6.14.0"
  }
}
